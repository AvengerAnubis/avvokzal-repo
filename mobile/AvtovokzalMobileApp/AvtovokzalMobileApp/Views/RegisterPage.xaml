<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:AvtovokzalMobileApp.ViewModels"
             x:Class="AvtovokzalMobileApp.Views.RegisterPage"
             x:DataType="vm:RegisterViewModel"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource Slate50}">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16" VerticalOptions="Center" MaximumWidthRequest="400">

            <Label Text="Регистрация" FontSize="28" FontAttributes="Bold"
                   HorizontalOptions="Center" TextColor="{StaticResource Primary}" />

            <BoxView HeightRequest="1" Color="{StaticResource Slate200}" Margin="0,8" />

            <Label Text="{Binding ErrorMessage}" TextColor="{StaticResource Error}"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullConverter}}"
                   BackgroundColor="{StaticResource ErrorLight}" Padding="12,8"
                   FontSize="14" />

            <Entry Placeholder="Имя *" Text="{Binding FirstName}"
                   BackgroundColor="{StaticResource White}"
                   PlaceholderColor="{StaticResource Slate400}" />

            <Entry Placeholder="Фамилия *" Text="{Binding LastName}"
                   BackgroundColor="{StaticResource White}"
                   PlaceholderColor="{StaticResource Slate400}" />

            <Entry Placeholder="Email *" Text="{Binding Email}" Keyboard="Email"
                   BackgroundColor="{StaticResource White}"
                   PlaceholderColor="{StaticResource Slate400}" />

            <Entry Placeholder="Телефон" Text="{Binding Phone}" Keyboard="Telephone"
                   BackgroundColor="{StaticResource White}"
                   PlaceholderColor="{StaticResource Slate400}" />

            <Entry Placeholder="Пароль *" Text="{Binding Password}" IsPassword="True"
                   BackgroundColor="{StaticResource White}"
                   PlaceholderColor="{StaticResource Slate400}" />

            <Entry Placeholder="Подтвердите пароль *" Text="{Binding ConfirmPassword}" IsPassword="True"
                   BackgroundColor="{StaticResource White}"
                   PlaceholderColor="{StaticResource Slate400}" />

            <Button Text="Зарегистрироваться" Command="{Binding RegisterCommand}"
                    BackgroundColor="{StaticResource Primary}" TextColor="White"
                    FontAttributes="Bold" HeightRequest="48"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />

            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                               Color="{StaticResource Primary}" HeightRequest="30" />

            <VerticalStackLayout HorizontalOptions="Center" Spacing="8">
                <Label Text="Уже есть аккаунт?" FontSize="14" TextColor="{StaticResource Slate500}"
                       HorizontalOptions="Center" />
                <Button Text="Войти" Command="{Binding GoToLoginCommand}"
                        BackgroundColor="Transparent" TextColor="{StaticResource Primary}"
                        FontSize="14" BorderColor="{StaticResource Primary}" BorderWidth="1" />
            </VerticalStackLayout>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>